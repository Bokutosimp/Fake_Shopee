# LOCAL testing only. NOT used by GZ::CTF (which orchestrates the image itself).
# Injects a test GZCTF_FLAG and maps the internal port 80 to host 8080.
services:
  shopstack:
    build: .
    image: shopstack:local
    environment:
      GZCTF_FLAG: "flag{local_test}"
    ports:
      - "8080:80"
